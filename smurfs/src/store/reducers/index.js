


const initialState = {
  smurfs: [
    {
      "name": "Test Subject 626",
      "age": 2,
      "height": "5cm",
      "id": 0
    }
  ]
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
        return state;
  }
};